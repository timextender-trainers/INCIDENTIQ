# OpenAI Configuration (Required for AI features)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Azure OpenAI Configuration (Optional - for enterprise deployment)
# Get these from your Azure OpenAI resource
AZURE_OPENAI_API_KEY=your_azure_openai_api_key_here
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/

# Database Configuration (matches Docker Compose setup)
# These credentials are used by Docker Compose to set up the local SQL Server container
DB_PASSWORD=IncidentIQ123!

# Application Settings
ASPNETCORE_ENVIRONMENT=Development

# Note: Database credentials are hardcoded in appsettings.Development.json 
# for local development convenience. In production, use secure connection strings
# with managed identities or secure credential storage.